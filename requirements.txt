# Deps for the training pipeline (process_data.py, engineer.py, train.py).
# api/requirements.txt has the separate, slimmer serving-only deps.
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
xgboost>=2.1.0
dvc[s3]>=3.38.0  # Data version control (datasets + models) with S3 remote
boto3>=1.34.0
mlflow>=2.16.0
joblib>=1.3.1
